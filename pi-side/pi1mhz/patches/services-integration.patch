diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 8ca5811..0b1b643 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -292,6 +292,8 @@ set(FrameBuffer_emulator_files
 set(services_emulator_files
    services_emulator.c
    fat_service.c
+   ftp_service.c
+   ftp_service.h
    net_service.c
    net_service.h
    net_tnfs.c
@@ -450,6 +452,8 @@ set( Pi1MHz_sources
     ${wifi_files}
     ${lwip_files}
     wifi_service.c
+    media_catalogue.c
+    media_catalogue.h
 )
 
 # These are cold (config/filename handling, ~12 call sites). We recompile them
diff --git a/src/services.h b/src/services.h
index 48b6a8d..5b5532e 100644
--- a/src/services.h
+++ b/src/services.h
@@ -30,7 +30,9 @@
 #define SERVICE_CMD_WIFI_LAST  93u
 #define SERVICE_CMD_SECURE_FIRST  94u /* RNG and managed SSH - secure_service.c */
 #define SERVICE_CMD_SECURE_LAST  113u
-/* 114..255 unallocated */
+#define SERVICE_CMD_FTP_FIRST    114u /* interactive FTP - ftp_service.c      */
+#define SERVICE_CMD_FTP_LAST     119u
+/* 120..255 unallocated */
 
 /* ---- untrusted-input bounds checks -------------------------------------
    Every service takes an offset and a length from the Beeb, so this is one
@@ -81,6 +83,8 @@ _Static_assert(SERVICE_CMD_FAT_LAST     < SERVICE_CMD_AUN_FIRST,     "FAT overla
 _Static_assert(SERVICE_CMD_AUN_LAST     < SERVICE_CMD_NET_FIRST,     "AUN overlaps net");
 _Static_assert(SERVICE_CMD_NET_LAST     < SERVICE_CMD_WIFI_FIRST, "net overlaps ElkWiFi");
 _Static_assert(SERVICE_CMD_WIFI_LAST < SERVICE_CMD_SECURE_FIRST,  "ElkWiFi overlaps secure");
+_Static_assert(SERVICE_CMD_FTP_FIRST     <= SERVICE_CMD_FTP_LAST,     "FTP range inverted");
+_Static_assert(SERVICE_CMD_SECURE_LAST  < SERVICE_CMD_FTP_FIRST,     "secure overlaps FTP");
 
 /* What a Beeb sees when a service is not there.  Three states, deliberately
    distinguishable, because a ROM has to tell them apart:
diff --git a/src/wifi_service.c b/src/wifi_service.c
index 713814b..fc41a2b 100644
--- a/src/wifi_service.c
+++ b/src/wifi_service.c
@@ -27,6 +27,7 @@
 #include "BeebSCSI/filesystem.h"
 #include "config.h"
 #include "wifi_service.h"
+#include "ftp_service.h"
 #include "uef_service.h"
 #include "ram_emulator.h"
 #include "services.h"
@@ -744,6 +745,10 @@ void wifi_service_init(uint8_t instance, uint8_t address)
    uef_service_reset();
    (void)services_register(WIFI_SVC_CMD_STATUS, WIFI_SVC_CMD_LAST,
                            wifi_service_command);
+   /* *FTP shares this service's lifetime: it is the same host ROM, the same
+      mailbox and the same enable key, and it has no state to keep when the
+      WiFi service is not there to associate. */
+   ftp_service_init();
    /* A host reset abandons the old OSWORD/star-command caller. Complete any
       request latched during reset reinitialisation with a bounded error before
       clearing it, so FCAA can never be left at BUSY. */
