IF SSH_REAL_SMOKE
    PUTFILE "build/SSHREALBOOT", "!BOOT", 0, 0
ELIF SSH_SMOKE
    PUTFILE "build/SSHBOOT", "!BOOT", 0, 0
ELSE
    PUTFILE "build/TERMBOOT", "!BOOT", 0, 0
ENDIF

PUTFILE "build/NETMENU", "NETMENU", &31900, &31900
PUTFILE "build/TERM", "TERM", &31900, &31900
PUTFILE "build/SSH", "SSH", &31900, &31900
